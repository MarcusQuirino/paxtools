---
"paxtools": minor
---

Add the `sandcastle` AFK backlog loop — an unattended session that plans the
`ready-for-agent` backlog into dependency order, then implements, gates and
reviews each ticket on one shared branch before opening a single pull request.
