---
"paxtools": patch
---

Fix long item text being cut off in the escotista "pendentes" tab. Each item's text is now a tappable control that toggles between a 2-line preview and the full text, so escotistas can read the whole entry. The checkbox keeps its own click target and accessible name.
